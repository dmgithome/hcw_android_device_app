# 耗材屋安卓版重构验收清单（登录 / 取用 / 归还）

> 说明：本文件保留为**历史记录**（包含当时的偏差与进度描述，易过期）。  
> 最新“稳定验收标准 / 进度记录 / PRD 骨架”请以以下文件为准：  
> - [docs/PRD.md](docs/PRD.md)  
> - [docs/验收标准_v2.md](docs/验收标准_v2.md)  
> - [docs/验收记录.md](docs/验收记录.md)

## 1. 文档目的

这份清单用于约束“耗材屋安卓版重构”必须与既有业务逻辑保持一致，避免功能看起来能用、但业务口径已经偏掉。

- 范围：仅覆盖本次重构目标的 3 个功能
  - 登录
  - 取用
  - 归还
- 验收基准：
  - `hcw_web_app`（前端行为）
  - `hcw_yao`（后端业务规则）
- 当前文档状态：已更新为“代码已完成（待统一测试）”版本（2026-02-24）

## 2. 结论先行（当前已确认）

当前安卓重构版本和既有业务相比，存在几个明显偏差，优先级最高的是：

1. NFC 登录被做成“手工输入”，但真实业务是“刷卡触发 MQTT 自动登录”。
2. 取用目标位置被做成“填写文本”，但真实业务是“从地点列表里选择”。
3. MQTT clientId 需要稳定持久化，当前安卓实现是随机值。
4. 取用/归还提交时的冲突处理（409）前端体验还不完整。

上面这几项不修正，不能算“1:1 复刻”。

## 3. 业务口径（白话版，必须一致）

### 3.1 登录（密码登录 + NFC 登录）

- 密码登录：
  - 先登录接口
  - 再拉用户信息
- NFC 登录：
  - 不是人工填 IP、填卡号
  - 是 MQTT 收到刷卡消息后自动触发
  - 需要把 `ReaderId(MAC)` 映射到设备，再取对应 `ipAddress`
  - NFC 卡号 `Value` 可能需要按规则转换（并且支持严格模式）
- 后端 NFC 登录真实口径：
  - 核心按 `cardNumber` 找人并登录
  - `IP` 当前不是校验核心条件

### 3.2 取用

- 开始盘点时，不是手输 IP 列表
- 应按 Web 逻辑：
  - 优先主柜 IP
  - 否则遍历全部设备 IP
- 取用目标位置：
  - 必须“选择”
  - 不是手填
  - 来源是地点列表（只显示房间类地点，并排除默认地点）
- 提交取用时：
  - 必须带 `to_location_id`
  - 后端会强校验
  - 要处理 409 冲突（位置不一致）

### 3.3 归还

- 归还时只允许提交“可归还”的条目（规则和 Web 一致）
- 提交每条需要带 `expected_current_location_id` 做并发校验
- 归还目标位置通常由后端反推，不是前端手选
- 需要处理 409 冲突（位置不一致）

## 4. 1:1 复刻清单（必须逐条通过）

> 说明：`当前安卓状态` 是基于本轮只读核对结论，后续每轮 loop 都要更新。

| ID | 1:1复刻要求（必须一致） | 验收步骤 | 通过标准 | 关键依据（源码） | 当前安卓状态 |
|---|---|---|---|---|---|
| L-01 | NFC 登录由 MQTT 自动触发，不是手工填 IP/卡号后点登录 | 发布 `dk25_nfc/card/#` 消息，含 `ReaderId` + `Value` | App 自动发起登录流程 | `/Users/dm/code/hcw_web_app/src/mqtt.ts:57`<br>`/Users/dm/code/hcw_web_app/src/mqtt.ts:82`<br>`/Users/dm/code/hcw_web_app/src/views/_builtin/login/components/NFC-Login/index.vue:1` | 未满足（当前需要手输） |
| L-02 | `ReaderId(MAC)` 必须先映射设备，再取 `ipAddress` 作为登录参数 | 设备表中存在该 MAC，刷卡 | 登录请求参数里的 IP 等于该设备 IP | `/Users/dm/code/hcw_web_app/src/store/modules/cabinet/index.ts:176`<br>`/Users/dm/code/hcw_web_app/src/store/modules/cabinet/index.ts:186`<br>`/Users/dm/code/hcw_web_app/src/service/api/cabinet.ts:4` | 未满足（当前无设备映射链路） |
| L-03 | NFC `Value` 支持格式转换（可开关，严格模式失败要拦截） | 开启严格模式，发送非法 `Value` | 提示转换失败，不继续登录 | `/Users/dm/code/hcw_web_app/src/mqtt.ts:66`<br>`/Users/dm/code/hcw_web_app/src/utils/nfc.ts:4` | 未满足 |
| L-04 | 后端 NFC 登录口径：按 `cardNumber` 找用户并 `LoginById`；IP 当前不参与校验核心逻辑 | 调 `/api/nfc/loginByNFC`，同卡号换不同 IP | 结果不因 IP 改变而变化 | `/Users/dm/code/hcw_yao/scripts/hv3/user/nfc.js:145`<br>`/Users/dm/code/hcw_yao/scripts/hv3/user/nfc.js:191`<br>`/Users/dm/code/hcw_yao/apis/hv3/nfc.http.yao:39` | 这是业务口径确认项 |
| L-05 | MQTT clientId 必须稳定持久化，不能每次随机 | 重启 App 两次，抓 MQTT 连接 | 两次 clientId 一致 | `/Users/dm/code/hcw_web_app/src/store/modules/system/index.ts:314`<br>`/Users/dm/code/hcw_web_app/src/store/modules/system/index.ts:319`<br>`/Users/dm/code/hcw_web_app/src/utils/mqtt/mqttClient.ts:61` | 未满足（当前随机） |
| L-06 | 密码登录链路：先登录，再获取用户信息 | 输入正确账号密码登录 | 登录成功并拿到用户信息 | `/Users/dm/code/hcw_web_app/src/service/api/auth.ts:34`<br>`/Users/dm/code/hcw_web_app/src/service/api/auth.ts:44` | 基本满足（需回归确认） |
| T-01 | 取用“开始盘点”：优先主柜 IP，否则全部设备 IP；逐 IP 调盘点接口 | 点击开始盘点 | 按规则发起盘点调用 | `/Users/dm/code/hcw_web_app/src/views/takeandreturn/readertake/index.vue:285`<br>`/Users/dm/code/hcw_web_app/src/service/api/stock.ts:31`<br>`/Users/dm/code/hcw_yao/apis/hv3/inventory.http.yao:11` | 部分满足（当前有手填倾向） |
| T-02 | 取用解析口径：只把 `inStockList` 加入可提交，其他状态走异常提示 | 准备 5 类样本扫码 | 分类结果与 Web 一致 | `/Users/dm/code/hcw_web_app/src/views/takeandreturn/readertake/index.vue:330`<br>`/Users/dm/code/hcw_yao/scripts/hv3/stock/stock.js:1201` | 部分满足（需完整回归） |
| T-03 | 取用目标位置必须“选择”，来源是地点列表（仅 room，排除默认地点） | 进入取用页 | 看到可选位置条，不是输入框 | `/Users/dm/code/hcw_web_app/src/views/takeandreturn/readertake/component/dialog.vue:4`<br>`/Users/dm/code/hcw_web_app/src/components/location/LocationStrip.vue:76`<br>`/Users/dm/code/hcw_web_app/src/components/location/LocationStrip.vue:80` | 未满足（当前是手输） |
| T-04 | 提交取用必须带顶层 `to_location_id`，后端强校验合法值 | 不选目标位置直接提交 | 前端拦截；后端也会拒绝非法值 | `/Users/dm/code/hcw_web_app/src/views/takeandreturn/readertake/component/dialog.vue:425`<br>`/Users/dm/code/hcw_yao/scripts/hv3/stock/stock.js:1671` | 部分满足（字段有，交互不对） |
| T-05 | 取用提交按柜体分组；409 冲突时要提示冲突信息 | 构造位置并发冲突后提交 | 前端能明确提示冲突 | `/Users/dm/code/hcw_web_app/src/views/takeandreturn/readertake/component/dialog.vue:401`<br>`/Users/dm/code/hcw_yao/scripts/hv3/stock/stock.js:1805` | 未满足（409处理不完整） |
| R-01 | 归还解析口径：只把 `taken + usedReturn` 加入可提交；其他状态走异常 | 准备 5 类样本扫码 | 分类结果与 Web 一致 | `/Users/dm/code/hcw_web_app/src/views/takeandreturn/readerreturn/index.vue:336`<br>`/Users/dm/code/hcw_yao/scripts/hv3/stock/stock.js:1353` | 基本满足（需回归确认） |
| R-02 | 归还提交每条需带 `expected_current_location_id` 做并发校验 | 提交前人为改库存位置后提交 | 后端返回 409“位置不一致” | `/Users/dm/code/hcw_web_app/src/views/takeandreturn/readerreturn/component/dialog.vue:460`<br>`/Users/dm/code/hcw_yao/scripts/hv3/stock/stock.js:1758` | 基本满足（前端提示需补齐） |
| R-03 | 归还目标位置由后端反推（最近 Take 来源地点，兜底当前地点），前端不手选 | 正常归还一条记录 | 归还成功且位置正确变化 | `/Users/dm/code/hcw_yao/scripts/hv3/stock/stock.js:1865`<br>`/Users/dm/code/hcw_yao/scripts/hv3/stock/stock.js:1870` | 基本满足（需完整验证） |
| C-01 | PRD 约束：外部 API、MQTT topic、业务规则不变 | 对照接口、topic 和流程 | 不新增/改名/删减对外口径 | `/Users/dm/code/hcw_android_device_app/docs/PRD.md:10`<br>`/Users/dm/code/hcw_web_app/src/settings/mqttTopic.ts:1` | 需持续守住 |

## 5. 详细验收标准（回归时逐条打勾）

下面这 14 条建议作为每轮 loop 的回归基线。每次改完都要标记“通过 / 不通过”，并写明证据。

### 5.1 登录相关

#### A-01 账号登录成功

- 前提：
  - 有可用账号密码
- 操作：
  - 输入正确账号密码并登录
- 期望：
  - 登录成功进入主界面
  - 能拿到用户信息
  - 页面不报错、不白屏

#### A-02 账号登录失败提示正确

- 前提：
  - 输入错误账号或密码
- 操作：
  - 点击登录
- 期望：
  - 给出清晰错误提示
  - 不进入主界面

#### A-03 NFC 自动登录成功（刷卡即登录）

- 前提：
  - MQTT 已连接
  - 设备表存在 `ReaderId(MAC)` 对应设备
- 操作：
  - 发布 `dk25_nfc/card/#` 消息，包含 `ReaderId` 和 `Value`
- 期望：
  - 不需要手工输入 IP/卡号
  - 自动发起登录流程并成功进入主界面

#### A-04 NFC 严格模式失败拦截

- 前提：
  - 开启 NFC 严格模式
- 操作：
  - 发送无法转换的 `Value`
- 期望：
  - 提示格式转换失败（或等价清晰提示）
  - 不继续登录请求

#### A-05 MQTT clientId 稳定

- 前提：
  - 已完成一次 MQTT 连接
- 操作：
  - 关闭 App -> 重开 -> 再次连接 MQTT
- 期望：
  - 两次连接使用相同 clientId

### 5.2 取用相关

#### A-06 取用开始盘点成功

- 前提：
  - 设备列表正常
  - MQTT 可接收盘点状态消息
- 操作：
  - 点击“开始盘点”
- 期望：
  - 按规则（主柜优先，否则全部设备）发起盘点
  - 在合理时间内进入“盘点中”状态

#### A-07 取用开始盘点超时处理

- 前提：
  - 人为模拟没有 ACK 返回
- 操作：
  - 点击“开始盘点”
- 期望：
  - 超时后给出可理解提示
  - 可以重试，不会卡死

#### A-08 取用扫码分类正确

- 前提：
  - 准备覆盖 5 类状态的测试样本
    - `inStock`
    - `taken`
    - `notInStock`
    - `consumed`
    - `usedReturn`
- 操作：
  - 扫码 / 盘点导入
- 期望：
  - 只有 `inStock` 进入“可提交列表”
  - 其余状态进入异常提示区域（文案可以不同，但分类必须一致）

#### A-09 取用目标位置必须选择

- 前提：
  - 打开取用提交界面
- 操作：
  - 不选择目标位置，直接提交
- 期望：
  - 前端阻止提交
  - 提示用户先选择目标位置
  - UI 是“选择控件”，不是输入框

#### A-10 取用提交成功

- 前提：
  - 有可提交的取用条目
  - 已选择目标位置
- 操作：
  - 提交取用
- 期望：
  - 按柜体分组提交
  - 返回成功后清空待提交列表
  - 页面状态恢复可继续操作

#### A-11 取用并发冲突（409）提示明确

- 前提：
  - 人为制造位置并发冲突
- 操作：
  - 提交取用
- 期望：
  - 后端返回 409 时，前端明确提示“位置不一致”类问题
  - 能定位到冲突条目（至少让用户知道有冲突，而不是泛化成普通失败）

### 5.3 归还相关

#### A-12 归还扫码分类正确

- 前提：
  - 准备覆盖 5 类状态的测试样本
- 操作：
  - 扫码 / 盘点导入
- 期望：
  - `taken + usedReturn` 进入“可提交列表”
  - `inStock / notInStock / consumed` 进入异常提示区域

#### A-13 归还并发校验（409）生效

- 前提：
  - 提交前人为修改条目当前位置
- 操作：
  - 提交归还
- 期望：
  - 请求携带 `expected_current_location_id`
  - 后端返回 409 时，前端给出明确冲突提示

#### A-14 归还成功链路完整

- 前提：
  - 有可归还条目
- 操作：
  - 提交归还
- 期望：
  - 归还成功
  - 列表清空
  - 页面可继续下一次操作
  - 不出现白屏、卡死、状态错乱

## 6. 已确认的安卓偏差清单（本轮只读核对结果）

### 6.1 登录偏差

1. NFC 登录仍要求手工输入 `NFC读卡IP` 和卡号，不符合“刷卡自动登录”口径。  
   依据：
   - `/Users/dm/code/hcw_android_device_app/app/src/main/java/com/hv/cabinet/feature/login/LoginScreen.kt:98`
   - `/Users/dm/code/hcw_android_device_app/app/src/main/java/com/hv/cabinet/feature/login/LoginViewModel.kt:185`

2. MQTT NFC 消息解析未按 Web 口径完整使用 `ReaderId + Value`，也缺少转换与严格模式逻辑。  
   依据：
   - `/Users/dm/code/hcw_android_device_app/app/src/main/java/com/hv/cabinet/data/mqtt/MqttManager.kt:254`
   - `/Users/dm/code/hcw_android_device_app/app/src/main/java/com/hv/cabinet/data/mqtt/MqttManager.kt:282`

3. MQTT clientId 为随机值，不符合稳定持久化要求。  
   依据：
   - `/Users/dm/code/hcw_android_device_app/app/src/main/java/com/hv/cabinet/data/mqtt/MqttManager.kt:75`
   - `/Users/dm/code/hcw_android_device_app/app/src/main/java/com/hv/cabinet/data/mqtt/MqttManager.kt:107`

### 6.2 取用偏差

4. 取用目标位置是手输文本，不是地点列表选择。  
   依据：
   - `/Users/dm/code/hcw_android_device_app/app/src/main/java/com/hv/cabinet/feature/take/TakeScreen.kt:95`
   - `/Users/dm/code/hcw_android_device_app/app/src/main/java/com/hv/cabinet/feature/take/TakeViewModel.kt:185`

5. 安卓侧尚未接入地点列表接口（当前 API 定义中未见 `/api/locations/list` 等）。  
   依据：
   - `/Users/dm/code/hcw_android_device_app/app/src/main/java/com/hv/cabinet/data/api/CabinetApi.kt:1`

### 6.3 提交与冲突处理偏差

6. `to_location_id`、`expected_current_location_id` 字段虽然存在，但 409 冲突提示体验还未按 Web 逻辑做完整处理（当前偏通用失败提示）。  
   依据：
   - `/Users/dm/code/hcw_android_device_app/app/src/main/java/com/hv/cabinet/data/api/ApiModels.kt:81`
   - `/Users/dm/code/hcw_android_device_app/app/src/main/java/com/hv/cabinet/data/api/CabinetRepository.kt:214`

## 7. 最新代码进度（2026-02-24，待统一测试确认）

### 7.1 已完成（代码层）

- 登录口径
  - NFC 登录改为 MQTT 刷卡自动触发（`ReaderId + Value`）
  - `ReaderId(MAC) -> 设备 -> IP` 自动映射
  - NFC `Value` 转换开关与严格模式
  - MQTT `clientId` 持久化
- 取用口径
  - 目标位置改为“选择”（地点列表，仅 `room`，排除默认地点）
  - 开始盘点不依赖目标位置选择
  - 提交取用仍强校验目标位置（未选时前端拦截）
  - 409 冲突提示补齐（含冲突 RFID 列表）
- 归还口径
  - 归还提交并发冲突（409）提示补齐（含冲突 RFID 列表）
  - 归还盘点启动口径与取用一致（主柜优先，否则全部设备）
- UI/交互复刻（安卓独立导航保留）
  - 登录页、首页、取用页、归还页改为 Web 风格深色背景 + 半透明面板交互
  - 取用目标位置改为 Web 风格位置选择条（选择，不填写）
  - 列表项信息层级与冲突高亮样式优化
- 性能优化（debug 体感向）
  - 高流量 RFID 消息批处理/节流（`80ms/20条`）
  - 默认关闭重型日志（HTTP BODY / Perf 日志开关已接入配置）
  - `PerfMonitor` 默认 no-op
  - 页面状态订阅切换为 `collectAsStateWithLifecycle`
- 稳定性
  - 修复 `LocalBroadcastManager` 运行时崩溃（Paho Android Service 兼容）

### 7.2 已验证（本地）

- `./gradlew :app:compileDebugKotlin` 通过
- `./gradlew :app:assembleDebug :app:assembleRelease` 通过
- `adb -s 192.168.31.134:5555 install -r app/build/outputs/apk/debug/app-debug.apk` 成功

### 7.3 待统一测试（现场环境）

- 本清单第 5 节功能验收用例（14 条）
- UI/交互复刻对照验收（逐屏截图/录屏比对 Web）
- debug 包性能体感验收（页面切换、盘点高频消息下可操作性）

## 8. 后续 Loop 执行建议（直到重构合格）

### 第 1 轮（P0，先修硬偏差）

目标：把最关键的业务口径拉回正确轨道。

- `L-01` NFC 自动登录（MQTT 触发）
- `L-02` MAC -> 设备 -> IP 映射
- `L-03` NFC Value 转换 + 严格模式
- `L-05` MQTT clientId 持久化
- `T-03` 取用目标位置改为“选择”

### 第 2 轮（P1，修提交口径与冲突处理）

- `T-04` 取用 `to_location_id` 校验与交互
- `T-05` 取用 409 冲突提示
- `R-02` 归还 409 冲突提示与体验补齐

### 第 3 轮（P2，全量回归）

- 按第 5 节 14 条验收标准逐条回归
- 输出“通过 / 不通过 / 证据”
- 未通过项进入下一轮修复

## 9. 每轮汇报模板（建议固定）

后续每次 loop 建议按下面格式汇报，方便对账：

```text
结论：本轮是否达到预期（通过了哪些条目）。
证据：改动文件 + 验证命令/操作步骤 + 结果（通过/失败）。
下一步：下一轮要处理的条目（按优先级）。
风险与回滚：本轮改动可能影响点，以及快速回退方式。
```

## 10. 备注（本文件生成说明）

- 本文件内容来自本轮“只读核对”结果整理。
- 目的不是给出实现方案，而是先把业务口径和验收标准定死，后续按清单反复 loop。
- 如 Web / Yao 后续代码有变更，需要同步更新本清单对应条目和证据路径。
